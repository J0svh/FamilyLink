{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.test.ts", "src/**/*.spec.ts"],
  "exec": "ts-node-dev --respawn --transpile-only src/infrastructure/http/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
